exec autoexec

// Enhanced ubercharge
bind mouse2 +charge

// Vocalise fake ubercharge call
bind mouse3 fakeuber

// Medic loadout switching
bind "KP_5" "swap_medigun"
bind "KP_UPARROW" "swap_spawn"

// Loadout switching
unbind "KP_END"
unbind "KP_DOWNARROW"
unbind "KP_PGDN"
unbind "KP_LEFTARROW"